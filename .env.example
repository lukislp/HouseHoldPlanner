# Copy this file to .env and fill in a real value.
# .env is NOT checked into the repository.

# Secret key used to sign JWT auth tokens - must be at least 32 characters.
# Generate one with: openssl rand -base64 32
JWT_KEY=
